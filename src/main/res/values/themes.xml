<?xml version="1.0" encoding="utf-8"?>
<resources>
<style name="Theme.Main" parent="Theme.AppCompat.Light">

        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/colorThemeMainPrimary</item>

        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/colorThemeMainPrimaryDark</item>

        <!-- colorAccent is used as the default value for colorControlActivated
        which is used to tint widgets -->
        <item name="colorAccent">@color/colorThemeMainAccent</item>

        <!-- You can also set colorControlNormal, colorControlActivated
        colorControlHighlight & colorSwitchThumbNormal. -->
        <item name="colorControlNormal">@color/colorThemeMainControlNormal</item>
        <item name="colorControlActivated">@color/colorThemeMainControlActivated</item>
        <item name="colorControlHighlight">@color/colorThemeMainControlHighlight</item>
        <item name="colorSwitchThumbNormal">@color/colorThemeMainSwitchThumbNormal</item>

        <!-- Default backgrounds -->
        <item name="android:background">@color/colorThemeMainBackground</item>
        <item name="android:windowBackground">@color/colorThemeMainWindowBackground</item>
        <item name="android:colorBackground">@color/colorThemeMainColorBackground</item>

    </style>
</resources>
